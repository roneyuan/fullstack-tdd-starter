import React from 'react';
import TestUtils from 'react-dom/test-utils';
import chai from 'chai';

const should = chai.should();

import YourComponent from '../views/js/components/yourComponent';

describe('YourComponent component', function() {
  it('Renders YourComponent',  function() {
    /* Test here */
    /* Example
    const name = "TEST";

    const renderer = TestUtils.createRenderer();
    renderer.render(<YourComponent  name={name} />);
    const result = renderer.getRenderOutput();
    result.props.className.should.equal('your_component_className');

    const div = result.props.children[0];
    div.type.should.equal('div');
    div.props.children.should.equal(name);
    */

  });
});